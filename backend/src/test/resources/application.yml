spring:
  datasource:
    url: jdbc:h2:mem:englishapp-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  jwt-secret: test-test-test-test-test-test-test-test
  jwt-expiration-minutes: 1440
